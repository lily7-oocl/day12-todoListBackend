server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/todo?useUnicode=true&characterEncoding=utf8
    username: root
    password: Q2AYA2n46a68*mi
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
  flyway:
    baseline-on-migrate: true